-- Create table Employees
CREATE TABLE Employees
(
    EmployeeID INT,
    Name       VARCHAR(50),
    Age        INT
);

-- Insert table Employees
INSERT INTO Employees (EmployeeID, Name, Age)
VALUES (1, 'Alice Brown', 35),
       (2, 'Bob White', 40),
       (3, 'Charlie Green', 28);